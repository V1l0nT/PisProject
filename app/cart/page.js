import Cart from "../components/CartPage";

/**
 * Страница корзины, отображающая компонент Cart.
 *
 * @component
 * @returns {JSX.Element} Разметка страницы корзины
 */
export default function CartPage() {
  return (
    <div>
      <Cart />
    </div>
  );
}
